# TongWeb/TongRDS 适配测试矩阵（可执行版）

## 1. 目标与适用范围

- 目标：验证当前框架在 `TongWeb + TongRDS` 组合下的功能一致性、稳定性与可运维性。
- 适用系统：`ahzx-wmt-svc`（Spring Boot 3.5.x / Java 17 / MyBatis-Plus / Druid / dynamic datasource）。
- 验证基线：与现有可运行环境（建议称为 E1）对比，目标环境称为 E2。

## 2. 执行约定

- 结果状态：`PASS` / `FAIL` / `BLOCKED`
- 当前状态：`已验证` / `待验证` / `环境受限`
- 优先级：
  - `P0`：阻断上线项（必须全通过）
  - `P1`：关键稳定性与兼容项（建议全通过）
  - `P2`：增强信心项（建议灰度前通过）
- 每条用例至少保留 1 项证据（日志、响应、截图、SQL结果）

## 3. 通用通过/失败标准

### 3.1 通过标准（总体验收）

- P0 用例通过率 = 100%
- 核心业务链路（登录、核心 CRUD、分页）结果与基线一致
- 事务语义正确（提交成功、回滚彻底）
- 稳态压测无连接池耗尽、无线程持续堆积
- 监控与日志可用于定位问题（health/日志/SQL统计可查）

### 3.2 失败判定（任一命中即不通过）

- 服务无法启动或关键 Bean 初始化失败
- 核心链路不可用、鉴权异常、响应结构不兼容
- 出现数据不一致、事务失效、主从路由错误
- 稳态运行出现连接池打满或错误率持续升高

## 4. 测试记录模板（执行时填写）


| 用例ID     | 当前状态 | 执行人 | 执行时间 | 环境(E1/E2) | 结果                | 缺陷ID | 证据链接 | 备注            |
| -------- | ---- | --- | ---- | --------- | ----------------- | ---- | ---- | ------------- |
| 示例：TW-01 | 环境受限 |     |      |           | PASS/FAIL/BLOCKED |      |      | TongWeb 环境未就绪 |


## 5.0 当前进展预填（按你已反馈）

> 已知事实（阶段结论）：
>
> 1. TongRDS 已可连接，系统可运行；
> 2. 业务已完成登录与 CRUD 操作验证；
> 3. TongWeb 因环境问题暂无法验证，当前阶段先到此为止。


| 用例ID  | 当前状态 | 结果      | 备注                              |
| ----- | ---- | ------- | ------------------------------- |
| TW-01 | 环境受限 | BLOCKED | TongWeb 环境未开放，无法在 TongWeb 运行面验证 |
| TW-02 | 环境受限 | BLOCKED | 同上                              |
| TW-03 | 环境受限 | BLOCKED | 同上（本地登录通过不等价于 TongWeb 代理链路通过）   |
| TR-01 | 已验证  | PASS    | 已确认 TongRDS 连通、系统可启动运行          |
| TR-02 | 已验证  | PASS    | 已完成 CRUD 操作验证                   |


其余 TongRDS 用例当前状态默认：`待验证`；其余 TongWeb 用例当前状态默认：`环境受限`

## 5.1 测试矩阵（已填充可执行）

### 5.1.1 TongWeb 适配


| ID    | 优先级 | 当前状态 | 适配域     | 用例名称          | 前置条件              | 执行步骤                                    | 预期结果                             | 通过标准            | 失败判定               | 证据记录                   |
| ----- | --- | ---- | ------- | ------------- | ----------------- | --------------------------------------- | -------------------------------- | --------------- | ------------------ | ---------------------- |
| TW-01 | P0  | 环境受限 | TongWeb | 应用冷启动         | 已部署到 TongWeb；配置可读 | 1) 冷启动 2) 看启动日志 3) 调 `/actuator/health` | 应用启动成功，health 为 UP               | 连续 3 次冷启动成功     | 任一次启动失败/关键 Bean 报错 | TongWeb 环境未就绪（BLOCKED） |
| TW-02 | P0  | 环境受限 | TongWeb | 应用热重启         | 应用已启动             | 1) 重启容器 2) 观察错误 3) 再次访问 health          | 重启后服务可用                          | 重启后 2 分钟内恢复正常   | 重启后持续报错/不可用        | TongWeb 环境未就绪（BLOCKED） |
| TW-03 | P0  | 环境受限 | TongWeb | 登录与鉴权链路       | 准备有效账号            | 1) 登录 2) 使用 token 访问受保护接口               | 登录成功、鉴权通过                        | 登录接口成功且鉴权接口 200 | 登录失败/鉴权失败          | TongWeb 环境未就绪（BLOCKED） |
| TW-04 | P0  | 环境受限 | TongWeb | 统一返回结构校验      | 选 3+ 核心接口         | 1) 调 CRUD 与分页接口 2) 核查响应结构               | 响应符合 `CommonResult`/`PageResult` | 字段与语义一致         | 出现裸 Map/结构变化       | TongWeb 环境未就绪（BLOCKED） |
| TW-05 | P1  | 环境受限 | TongWeb | 全局异常处理一致性     | 可触发业务异常           | 1) 构造非法参数 2) 调接口触发异常                    | 返回业务错误语义，非裸 500                  | 与基线错误码/错误语义一致   | 异常栈泄露/错误处理失真       | TongWeb 环境未就绪（BLOCKED） |
| TW-06 | P1  | 环境受限 | TongWeb | 文件上传下载        | 准备小/中/大测试文件       | 1) 上传 2) 下载 3) 校验 hash                  | 上传下载成功、内容一致                      | 3 种文件均通过        | 上传失败/文件损坏          | TongWeb 环境未就绪（BLOCKED） |
| TW-07 | P1  | 环境受限 | TongWeb | 中文编码一致性       | 准备中文请求数据          | 1) 提交中文字段 2) 再查询回显                      | 无乱码、无截断                          | 中文字段存取正确        | 乱码/截断/字段异常         | TongWeb 环境未就绪（BLOCKED） |
| TW-08 | P1  | 环境受限 | TongWeb | Actuator 端点可用 | 已启用管理端点           | 1) 访问 health/info                       | 端点可访问且返回正常                       | 核心端点均正常         | 端点不可访问/状态异常        | TongWeb 环境未就绪（BLOCKED） |
| TW-09 | P1  | 环境受限 | TongWeb | Druid 监控可用    | Druid 监控开启        | 1) 访问 `/druid/*` 2) 查看 SQL 统计           | 页面可访问，统计可见                       | 可登录并看到连接/SQL信息  | 页面错误/数据空白异常        | TongWeb 环境未就绪（BLOCKED） |
| TW-10 | P1  | 环境受限 | TongWeb | 稳态资源观察        | 压测工具就绪            | 1) 30~60 分钟典型流量压测 2) 看线程与连接             | 资源稳定，错误率可控                       | 错误率不高于基线；无泄漏    | 连接池耗尽/线程堆积         | TongWeb 环境未就绪（BLOCKED） |
| TW-11 | P2  | 环境受限 | TongWeb | 会话保持/失效       | 多端可操作             | 1) 登录后多次访问 2) 登出后再访问                    | 会话行为符合预期                         | 登录后有效、登出后失效     | 会话异常持久或误失效         | TongWeb 环境未就绪（BLOCKED） |
| TW-12 | P2  | 环境受限 | TongWeb | 升级回滚演练        | 准备旧版本包            | 1) 升级 2) 验证 3) 回滚 4) 再验证                | 回滚后服务恢复                          | 回滚流程可重复执行       | 回滚失败/服务异常          | TongWeb 环境未就绪（BLOCKED） |


### 5.1.2 TongRDS 适配


| ID    | 优先级 | 当前状态 | 适配域     | 用例名称        | 前置条件                   | 执行步骤                            | 预期结果                  | 通过标准          | 失败判定          | 证据记录           |
| ----- | --- | ---- | ------- | ----------- | ---------------------- | ------------------------------- | --------------------- | ------------- | ------------- | -------------- |
| TR-01 | P0  | 已验证  | TongRDS | JDBC 连接连通性  | 已配置 TongRDS 连接参数       | 1) 启动应用 2) 调简单查询接口              | 连接成功，无驱动协议异常          | 启动期与运行期连接正常   | 驱动/认证/协议错误    | 已验证：系统可启动运行    |
| TR-02 | P0  | 已验证  | TongRDS | 基础 CRUD 一致性 | 准备测试实体数据               | 1) 增 2) 查 3) 改 4) 删             | 结果与基线一致               | CRUD 全部成功     | 任一操作失败或数据错乱   | 已验证：CRUD 可正常操作 |
| TR-03 | P0  | 待验证  | TongRDS | 事务提交验证      | 有事务成功路径                | 1) 调成功事务场景 2) 查库                | 事务内数据完整提交             | 提交后数据完整可见     | 提交不完整/丢写      | DB 校验截图        |
| TR-04 | P0  | 待验证  | TongRDS | 事务回滚验证      | 有异常回滚路径                | 1) 触发异常 2) 查库                   | 事务完全回滚                | 无中间脏数据        | 部分写入/脏数据残留    | 错误日志 + DB 快照   |
| TR-05 | P0  | 待验证  | TongRDS | 唯一约束冲突      | 目标表有唯一索引               | 1) 写入重复唯一键数据                    | 返回可识别业务错误             | 错误语义可被业务识别    | 误成功/异常不可识别    | 响应 + DB 状态     |
| TR-06 | P0  | 待验证  | TongRDS | 主从路由正确性     | dynamic datasource 已开启 | 1) 发起写请求 2) 发起读请求 3) 看路由落点      | 写走 master，读按策略走 slave | 与设计一致         | 错库读写/写入 slave | SQL 审计/路由日志    |
| TR-07 | P1  | 待验证  | TongRDS | 分页 SQL 兼容   | 准备多页数据                 | 1) 查询第 1/2/末页 2) 校验 total       | 分页与 total 正确          | 无重复、无丢失、无错位   | 页错位/total 不正确 | 分页响应记录         |
| TR-08 | P1  | 待验证  | TongRDS | 排序与过滤条件兼容   | 准备可排序/可过滤数据            | 1) 多字段排序 2) 范围 + 模糊过滤           | 排序/过滤结果正确             | 与基线结果一致       | 顺序错误/过滤失效     | 对比结果截图         |
| TR-09 | P1  | 待验证  | TongRDS | 批量写入兼容      | 准备批量数据                 | 1) 执行批量插入/更新 2) 核对条数            | 批量执行稳定、结果正确           | 成功率 100%，条数一致 | 部分失败/条数不一致    | 执行日志 + 条数校验    |
| TR-10 | P1  | 待验证  | TongRDS | 慢 SQL 可观测   | 可构造慢查询                 | 1) 触发慢 SQL 2) 检查 Druid/日志       | 可观测并可追踪               | 慢 SQL 记录完整    | 记录缺失/信息不全     | 慢 SQL 日志截图     |
| TR-11 | P1  | 待验证  | TongRDS | 连接池稳态       | 压测工具就绪                 | 1) 并发 30 分钟 2) 观察连接池            | 无泄漏/无长期超时             | 连接数在阈值内稳定波动   | 池耗尽/频繁超时      | 监控图 + 错误统计     |
| TR-12 | P2  | 待验证  | TongRDS | 断连恢复能力      | 可模拟网络抖动                | 1) 人为断连 10~30 秒 2) 恢复网络 3) 重试请求 | 自动恢复并继续服务             | 恢复后核心接口成功     | 长时间不可恢复       | 注入记录 + 恢复日志    |
| TR-13 | P2  | 待验证  | TongRDS | 字符集与时区一致性   | 有中文/时间字段数据             | 1) 写入中文与时间 2) 读取校验              | 无乱码、无时区偏移异常           | 与基线语义一致       | 乱码/时间偏差       | DB 查询 + 响应     |
| TR-14 | P2  | 待验证  | TongRDS | 大结果集读取稳定性   | 准备大数据集                 | 1) 大分页/批量读取 2) 观察内存与耗时          | 可稳定完成，无 OOM           | 连续执行无致命异常     | OOM/严重超时      | 日志 + 监控截图      |


## 6. 建议执行顺序

1. 先执行全部 P0：`TW-01~TW-04` + `TR-01~TR-06`
2. P0 全通过后执行 P1：稳定性、兼容性、性能观测
3. 最后执行 P2：故障注入、回滚演练、边界场景

## 7. 责任建议（可选）

- 开发：接口结构、异常语义、事务行为
- DBA：SQL兼容、主从路由、慢SQL与连接池
- 运维：TongWeb部署、回滚、监控告警
- 测试：用例执行、证据归档、缺陷跟踪


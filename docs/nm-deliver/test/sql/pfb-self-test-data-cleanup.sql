-- =============================================================================
-- PFB 开发自测 — 清除种子数据（仅删除固定 UUID，与 seed 脚本成对使用）
-- 使用方式：mysql ... wmt_pfb < docs/nm-deliver/test/sql/pfb-self-test-data-cleanup.sql
-- =============================================================================

SET NAMES utf8mb4;

DELETE FROM `pfb_loan_product` WHERE `id` IN (
  '11111111-1111-1111-1111-111111111101',
  '11111111-1111-1111-1111-111111111102',
  '11111111-1111-1111-1111-111111111103'
);

DELETE FROM `pfb_banner` WHERE `id` IN (
  '22222222-2222-2222-2222-222222222201',
  '22222222-2222-2222-2222-222222222202'
);

-- 若自测曾写入融资申请且 product_id 指向上述产品，可先手工删或按业务库清理策略处理：
-- DELETE FROM `pfb_loan_application` WHERE `product_id` LIKE '11111111-1111-1111-1111-11111111%';
